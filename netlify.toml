[[redirects]]
  from = "/neighborhoods.html"
  to = "/cities.html"
  status = 301
  force = true

[[redirects]]
  from = "/primequity.html"
  to = "/relifeiq.html"
  status = 301
  force = true

[[redirects]]
  from = "/thank-you"
  to = "/index.html"
  status = 301
  force = true

[[redirects]]
  from = "/thank-you.html"
  to = "/index.html"
  status = 301
  force = true

# ── Vanity / link-in-bio short URLs (speakable in videos & captions) ──
# /start, /search, /rebalance already resolve natively as clean URLs
# (Netlify serves start.html/search.html/rebalance.html at those paths),
# so only the ones whose clean path differs from the filename need a rule.
[[redirects]]
  from = "/value"
  to = "/what-is-my-home-worth.html"
  status = 301
  force = true
[[redirects]]
  from = "/newbuild"
  to = "/nc-report.html"
  status = 301
  force = true
[[redirects]]
  from = "/call"
  to = "/book.html"
  status = 301
  force = true
[[redirects]]
  from = "/links"
  to = "/start"
  status = 301
  force = true

# ── Private owner-update video pages (speakable short links) ──
[[redirects]]
  from = "/6310-update"
  to = "/updates/6310-star-light-ct/"
  status = 301
  force = true
[[redirects]]
  from = "/2222-update"
  to = "/updates/2222-scenic-shore/"
  status = 301
  force = true

# Internal strategy/working docs: keep in the repo, but 404 on the live
# site so they aren't publicly reachable (a robots rule would just list them).
[[redirects]]
  from = "/CLAUDE.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/COMMAND_CENTER.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/DEPLOY.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/PROJECT_CONTEXT.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/KEYWORD-STRATEGY.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/keyword-list.csv"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/GOOGLE-POSTS-CALENDAR.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/GOOGLE-PROFILE-RANKING-STRATEGY.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/SITEMAP-SUBMISSION-GUIDE.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/WEBSITE-WRAP-UP-CHECKLIST.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/idx-wrapper-instructions.txt"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/listing-template.html"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/FRONT-PORCH-OPS.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  from = "/AGENTIC_OS_INTEGRATION.md"
  to = "/404.html"
  status = 404
  force = true
[[redirects]]
  # Retired duplicate of incentives.html (superseded by the NewBuildIQ flow).
  from = "/nc-incentives.html"
  to = "/incentives.html"
  status = 301
  force = true
[[redirects]]
  # Deleted internal preview scratch file.
  from = "/har-preview.html"
  to = "/404.html"
  status = 404
  force = true

# No SPA catch-all: this is a static multi-page site, so unknown paths
# must return a real 404 (Netlify auto-serves /404.html) instead of the
# homepage with a 200 — avoids soft-404s and duplicate-content noise.

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# ── The Front Porch (members' area): keep it out of search entirely ──
# Header rather than a robots.txt Disallow, for two reasons:
#   1. robots.txt is public — listing the paths advertises exactly what's
#      private (same reasoning as the .md 404 rules above).
#   2. Disallow BLOCKS crawling, which means the noindex tag is never read.
#      A URL discovered elsewhere could still be indexed as a bare link.
# X-Robots-Tag is read on fetch and is authoritative. The pages also carry
# a <meta name="robots"> tag as belt-and-braces.
# NOTE: this is search hygiene, NOT access control. Real member data must be
# rendered server-side behind auth — never baked into a static file.
# Explicit paths, NOT /front-porch* — the wildcard also matched
# front-porch-pass.html, which is the one page in this family that IS meant to
# be indexed. An X-Robots-Tag header beats a page <meta robots>, so the wildcard
# would have silently overridden it and the page would never have ranked.
[[headers]]
  for = "/front-porch.html"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"
    Referrer-Policy = "no-referrer"

[[headers]]
  for = "/front-porch-pass.html"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"
    Referrer-Policy = "no-referrer"

[[headers]]
  for = "/front-porch-vendors.html"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet"
    Referrer-Policy = "no-referrer"

# Cache images for a week (satisfies PageSpeed "efficient cache lifetimes").
# CSS/JS are intentionally left on Netlify's revalidate default so edits go
# live instantly during active development.
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[build]
  publish = "."

[functions]
  included_files = ["data/**"]
